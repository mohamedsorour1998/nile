import React from 'react';

const Footer = () => (
  <footer className="text-center py-3 mt-5 border-top">
    <small className="text-muted">Nile &copy; {new Date().getFullYear()}</small>
  </footer>
);

export default Footer;
